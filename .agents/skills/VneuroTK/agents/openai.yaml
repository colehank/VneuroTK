interface:
  display_name: "VneuroTK"
  short_description: "Repo-specific patterns and workflows for VneuroTK"
  default_prompt: "Use the VneuroTK repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true